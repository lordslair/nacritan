apiVersion: v1
kind: Service
metadata:
  name: nacritan-sqlite-web
  labels:
    tier: nacritan-backend
spec:
  selector:
    app: python
    tier: nacritan-backend
  ports:
  - protocol: TCP
    port: 5001
