apiVersion: v1
kind: Service
metadata:
  name: nacritan-lb
  labels:
    tier: nacritan-backend
spec:
  type: LoadBalancer
  selector:
    app: python
    tier: nacritan-backend
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
